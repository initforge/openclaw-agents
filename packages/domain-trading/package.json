{
  "name": "@openclaw/domain-trading",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src",
    "test": "vitest run",
    "test:unit": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@openclaw/contracts": "workspace:*",
    "@openclaw/risk-engine": "workspace:*",
    "@openclaw/signal-parser": "workspace:*"
  }
}
